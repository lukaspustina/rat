---
version: build-{build}
environment:
  matrix:
  - TARGET: x86_64-pc-windows-msvc
    BITS: 64
  - TARGET: i686-pc-windows-msvc
    BITS: 32
  - TARGET: x86_64-pc-windows-gnu
    BITS: 64
  - TARGET: i686-pc-windows-gnu
    BITS: 32
install:
  - ps: Start-FileDownload "https://static.rust-lang.org/dist/rust-1.16.0-${env:TARGET}.exe" -FileName "rust-stable.exe"
  - rust-stable.exe /VERYSILENT /NORESTART /DIR="C:\rust"
  - ps: $env:PATH="$env:PATH;C:\rust\bin"
  - ps: $env:PATH="$env:PATH;C:\MinGW\bin"
  - rustc -V
  - cargo -V
build: off
test_script:
  # Compile, run test, check if the binary works, build release binaries
  - cargo test
  - cargo run -- --help
  - cargo build --release

